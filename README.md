# MyTodo

MyTodo 是一个轻量级、响应式且灵活的**纯静态**任务管理应用。它完全基于浏览器本地存储运行，无需配置后端数据库，无需安装任何依赖。

## ✨ 主要功能

- **无后端依赖**：纯静态网页，数据存储在浏览器 LocalStorage 中。
- **现代化 UI**：简洁清爽的界面，支持系统/浅色/深色主题切换。
- **任务管理**：支持任务创建、分类、优先级、截止日期和归档管理。
- **数据安全**：支持手动导出/导入 JSON 备份，数据完全掌握在自己手中。
- **多语言**：内置中英文切换。

## 🚀 部署与运行

由于本项目是纯静态网站，你可以将其部署到任何静态托管服务。我们推荐使用 **Cloudflare Pages**，它是免费且高性能的全球 CDN 托管服务。

### ☁️ Cloudflare Pages 部署 (推荐)

最简单的部署方式，只需几个步骤：

1. **准备代码**：
   将本项目代码上传到你的 GitHub 仓库。

2. **连接 Cloudflare**：
   - 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)。
   - 进入 **Workers & Pages** -> **Create application** -> **Pages** -> **Connect to Git**。

3. **配置构建**：
   - 选择你的 GitHub 仓库。
   - **Build command** (构建命令): `(留空)`
   - **Build output directory** (构建输出目录): `public`
   - 点击 **Save and Deploy**。

只需几秒钟，你的应用就会上线，并获得一个类似 `https://your-project.pages.dev` 的全球访问域名。

### 💻 本地运行

如果你想在本地预览：

1. 进入 `public` 文件夹。
2. 使用任何静态服务器打开。例如使用 Python：
   ```bash
   cd public
   python3 -m http.server 8000
   ```
3. 访问 `http://localhost:8000`。

## 📂 项目结构

```
.
├── public/             
│   ├── index.html      # 入口文件
│   ├── style.css       # 样式表
│   └── app.js          # 逻辑控制
└── README.md           # 说明文档
```

## 🛠 技术栈

- **Core**: HTML5, CSS3, Vanilla JavaScript
- **Storage**: LocalStorage API
- **Icons**: Lucide Icons (CDN)
- **Fonts**: Google Fonts (Inter, Poppins)

## 📝 许可证

MIT
